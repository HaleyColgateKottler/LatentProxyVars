---
title: "true_diff"
format: html
---

```{r}
tag = "1"
load(paste("param_", tag, ".RData", sep = ""))
```

```{r}
lambda_unique_inv = solve(lambda_unique)
H_covar_inv = solve(H_covar)
lambda_psi = t(lambda_common) %*% lambda_unique_inv

z = rep(1,p)

CATE = (C1-C2) %*% solve(lambda_psi %*% lambda_common + H_covar_inv) %*% (lambda_psi %*% z + H_covar_inv %*% H_mean)
CATE
```

```{r}
ATE = (C1-C2) %*% H_mean

ATE
```

